<div class="container mt-2">
  <div class="row mb-2 ml-1 mr-1 justify-content-between ">
    <div class="text">
      <h1><%= @mower.title %></h1>
      <div>
        <p class="price"> CHF <%= @mower.price_per_day %> per Day</p>
      </div>
      <div class="description">
        <p> <%= @mower.description %> </p>
          <p class="danger">
            <i class="fas fa-exclamation-triangle"></i>
            According to our terms and conditions, fuel tank is full when renter is taking the mower
            and so should it be returned.
          </p>
        <i class="fas fa-thumbs-up"></i> <%= rand(1..100) %>
        <i class="fas fa-thumbs-down"></i> <%= rand(0..50) %>
      </div>
      <div class="mt-3 mb-2">
        <%= link_to "Back to list", mowers_path, class: 'button' %>
      </div>
    </div>
    <div>
      <%= cl_image_tag @mower.photo.key, class:"image shadow rounded" %>
        <img src="https://i.pravatar.cc/50", class="avatar card_top"/>
    </div>
  </div>
  <div class="row">
    <div class="map col shadow">
      <div id="map" data-mapbox-key="<%= ENV["MAPBOX_API_KEY"] %>" data-markers="<%= @marker.to_json %>"></div>
    </div>
    <div class="col">
      <div class="bg-white rounded p-3 shadow">
        <h3>Book a mower !</h3>
        <h2><%= @mower.title %></h2>
        <%= simple_form_for [@mower, @booking] do |f| %>
          <%= f.input :pickup_date %>
          <%= f.input :return_date %>
          <%= f.submit class: 'button' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
